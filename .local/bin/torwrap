#!/bin/sh

ifinstalled stig transmission-cli || exit 1

! pgrep -x transmission-daemon >/dev/null && transmission-daemon && notify-send "Starting torrent daemon..."

$TERMINAL -e stig; pkill -72 "${STATUSBAR:-dwmblocks}"
